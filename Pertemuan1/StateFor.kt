fun StateFor (){
  val ranges = 1.rangeTo(10) step 3
  
  //i => variabel yang dipakai didalam loop
  //ranges => array
  val ranges = 1.rangeTo(10) step 3

  for (i in ranges ){
    println("value is $i!")
  }
}